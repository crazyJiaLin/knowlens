# ========================================
# 阶段1：获取 SSL 证书前的配置（当前使用）
# ========================================
# 使用说明：
# 1. DNS 解析生效后，使用此配置部署
# 2. 通过 Let's Encrypt 申请证书
# 3. 证书获取成功后，注释掉下面的配置，启用"阶段2"的配置

# HTTP server - 用于申请证书和临时访问
server {
    listen 80;
    server_name mikestan.cn www.mikestan.cn knowlens.mikestan.cn;
    root /usr/share/nginx/html;
    index index.html;
    
    # Let's Encrypt 验证路径
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    
    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json;
    
    # 前端静态文件
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # API 反向代理
    location /api {
        proxy_pass http://backend:3000/api;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}


# ========================================
# 阶段2：获取 SSL 证书后的配置（证书获取后启用）
# ========================================
# 使用说明：
# 1. 证书申请成功后，注释掉上面"阶段1"的整个 server 块（第10-58行）
# 2. 取消注释下面的两个 server 块（第68-149行）
# 3. 重新构建和部署前端容器

# HTTP server - 重定向到 HTTPS
# server {
#     listen 80;
#     server_name mikestan.cn www.mikestan.cn knowlens.mikestan.cn;
#     
#     # Let's Encrypt 验证路径（续期时需要）
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#     
#     # 其他请求重定向到 HTTPS
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }
# 
# # HTTPS server
# server {
#     listen 443 ssl http2;
#     server_name mikestan.cn www.mikestan.cn knowlens.mikestan.cn;
#     root /usr/share/nginx/html;
#     index index.html;
# 
#     # SSL 证书配置
#     ssl_certificate /etc/letsencrypt/live/mikestan.cn/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/mikestan.cn/privkey.pem;
#     
#     # SSL 优化配置
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
# 
#     # Gzip 压缩
#     gzip on;
#     gzip_vary on;
#     gzip_min_length 1024;
#     gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json;
# 
#     # 前端静态文件
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
# 
#     # API 反向代理
#     location /api {
#         proxy_pass http://backend:3000/api;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_cache_bypass $http_upgrade;
#         
#         # 超时设置
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
# 
#     # 静态资源缓存
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }
# }
